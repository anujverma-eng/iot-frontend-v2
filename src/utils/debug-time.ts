// Debug helper for DateTime picker
import { Time } from "@internationalized/date";

export const debugTimeRangeSelector = (
  action: string, 
  timeRange: { start: Date; end: Date }, 
  pendingStartTime?: Time, 
  pendingEndTime?: Time
) => {
  // Debug function disabled
};